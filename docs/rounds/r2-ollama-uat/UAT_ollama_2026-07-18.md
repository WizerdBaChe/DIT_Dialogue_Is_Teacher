# R2 Ollama 講解品質 UAT｜2026-07-18

## 結論

`qwen2.5-coder:7b` 已用與 2026-07-17 相同的真實 DIT session 重跑；前 10 節點嚴格評分 **7/10**，達到 PSM 的內容品質門檻。批次執行、進度與停止流程正常，未出現整頁崩潰。繁中輸出曾混入簡體字，prompt 加強後的三個代表性樣本已全數改為臺灣繁中。

R2 local 已完整通過。timeout／disconnect 除自動化測試外，使用者亦已在 Windows 工作管理員終止 `ollama.exe`，確認 UI 顯示可讀錯誤；重新啟動後「重新檢查」可正常恢復 ready。

## 測試環境

- 模型：`qwen2.5-coder:7b`，Ollama 本機安裝 4.7 GB。
- 真實資料：`41c6250e-c55e-475b-90ab-8fe0cb51fb03.jsonl`；解析後 88 spans、3 groups、41 view items。
- 設定：繁體中文、keep-alive 10 分鐘、輸出上限 512 token。
- 隱私：本輪只呼叫 `127.0.0.1:11434`，session 未送往雲端。

## 真實批次結果

- 首 30 秒完成 8/41；停止指令於當前請求完成後生效，最後停在 24/41。
- 進度列、停止狀態與已產生講解皆保留；頁面無崩潰。
- prompt 加強後的三個暖機樣本耗時 1.75–2.26 秒／節點。

## 前 10 節點嚴格評分

| # | 節點 | 判定 | 理由 |
|---:|---|---|---|
| 1 | 使用者意圖：評估 DIT 規劃 | 通過 | 正確辨識架構／實作缺口評估，why 能連到後續實作。 |
| 2 | `Skill` tool use | 通過 | 相較 3B，已正確辨識正在套用產品設計思考方法。 |
| 3 | skill 注入內容 | 未通過 | 誤解成只是在顯示技能目錄，未辨識方法規範已注入。 |
| 4 | 盤點專案狀態 | 通過 | 正確說明先取得現況再評估。 |
| 5 | `ls` 專案根目錄 | 通過 | 正確連到資源／結構盤點。 |
| 6 | Read product direction | 通過 | 明確辨識產品方向文件對目標與限制評估的用途。 |
| 7 | `ls` docs | 通過 | 能連到查找評估所需文件，未虛構命令。 |
| 8 | Read RPD | 未通過 | 仍只泛稱讀檔，未辨識需求／決策鎖定用途。 |
| 9 | Read architecture | 未通過 | 雖能連到技術架構，卻虛構「機器學習架構」限定。 |
| 10 | Read PROGRESS | 通過 | 正確連結進度文件與目前狀態。 |

總分：**7/10，通過內容品質門檻**。

## 語言與錯誤路徑

- 首輪出現 `产品／设计／项目／确保` 等簡體字；`prompt.ts` 已加上「只使用臺灣繁體中文，不得混入簡體字」。
- 加強後重跑 `Skill`、Read product direction、Read architecture 三個代表性節點，輸出皆為繁體字；模型仍可能在個別輪次補入未提供的目的，因此不再堆疊節點特例。
- `src/core/llm/ollama.test.ts` 固定 timeout 與 disconnect 的繁中錯誤訊息；與 prompt 測試合跑為 5/5 通過。

## PSM 驗收對照

| 驗收項目 | 結果 |
|---|---|
| 10 節點中 ≥7 個正確且非復述 | **通過：7/10** |
| 無整頁崩潰 | **通過** |
| 逾時／斷線提示可讀 | **通過：自動化測試＋使用者實機終止／恢復 `ollama.exe`** |
| 一頁品質紀錄 | **已產出本文件** |

## 人工錯誤路徑補驗｜2026-07-18

- 關閉工作列／應用程式視窗後，Ollama Windows 背景 server 仍可達，DIT 維持連線屬正常行為。
- 由工作管理員終止 `ollama.exe` 後，DIT 顯示預期的離線錯誤提示。
- 重新啟動 Ollama 後按「重新檢查」，狀態正常恢復。
- 使用者確認 OpenCode 面板狀態正常。

因此 PSM 三項 R2 UAT 驗收均有實機證據，R2 於 2026-07-18 關閉。
