# R9 — Session Browser + 狀態機統整：UAT 驗收清單

> 狀態：**已 merge 進 `main`**（作者裁決「有問題我之後修」）。分支 `feat/r9-session-browser-and-fsm` 保留未刪。
> **尚未 push、尚未建 PR、尚未打 release。**
> 上游：[`PSM_R9_WORKCARDS_v0.1.md`](./PSM_R9_WORKCARDS_v0.1.md)、[`docs/design/DIT_STATE_MACHINES.md`](../../design/DIT_STATE_MACHINES.md)
>
> 自動化狀態：`npm test` 350 passed / 46 files、`npm run build` 全綠（tsc + 兩個 vite build）。
> 下方每一項都標了「我已驗證到哪」與「為什麼還需要你」——**沒有標示的就是我沒驗過，不要當作已驗**。

---

## 0. 啟動

```bash
npm run dev
```

準備好 `C:\Users\gunda\.claude\projects\`。第一次開會跳歡迎彈窗，選完語言/講解模式關掉即可。

---

## A. 核心路徑（對應你回報的四個問題）

### A1 — 選一個專案目錄，看得到清單 ▸ 問題 3

1. 點右上「載入 Session 資料夾」。
2. 在系統目錄選擇器選 `C:\Users\gunda\.claude\projects\C--Users-gunda--claude`。

**預期**：出現「選擇要看的 Session」清單，約 50 筆，每筆有**可讀標題**（不是 HASH）、專案路徑、時間、`N 問 / M 答`、大小、分類徽章。

**我已驗證**：索引器對真實 218 個檔案跑過——109 個 session、108 個有可讀標題、耗時約 450 ms、0 診斷。
**為什麼還需要你**：目錄選擇器需要真人手勢，自動化無法觸發；我是用注入的目錄來源驗證索引與 UI，沒有走過真正的 `showDirectoryPicker()`。

---

### A2 — 那個原本讀不到的資料夾 ▸ 問題 1

1. 在清單中找 `66c03ab2…`（標題應為「剛剛做了系統環境的改動，給我一些測試驗證規則層路由跟 permissions.ask 有生效。」）。
2. 確認它**只出現一次**（不是主檔與子代理各一列），右側顯示「子代理 1」。
3. 點它載入。

**預期**：載入成功、結構樹裡有子代理分支、**不跳任何強制彈窗**。

**我已驗證**：用真實檔案跑過 pipeline——140 spans、2 個子代理群組、`.meta.json` 只產生一條 warn 而非整批失敗。
**為什麼還需要你**：以上是在 Node 測試環境跑的；瀏覽器的 Web Worker 串流路徑我只用合成資料驗過。

---

### A3 — 被誤判的那份對話 ▸ 問題 2 / 3

1. 在清單中找 `68466cc1…`。
2. 確認徽章是「**對話**」（滑過去 tooltip 應顯示「有真人輸入的訊息」）。
3. 載入它。

**預期**：**不跳解析提醒彈窗**。（原本會跳，是因為那 2 行 `stop_hook_summary` 被當成未知型別。）

**我已驗證**：全量掃描 140 個真實檔案後 `UNKNOWN_RECORD_TYPE` 為 0、`fatal` 為 0；分類器有一條以 `68466cc1` 真實形狀寫的負向對照測試。

---

### A4 — 壓縮標記看得到 ▸ 問題 2

1. 在清單中找一筆右側標有「**含壓縮**」的 session（真實資料裡只有 1 筆，你可能要捲一下）。
2. 載入它，在閱讀模式往下找。

**預期**：時序中出現一張標記卡，內容類似「對話歷史在此處被手動壓縮，壓縮前約 N tokens；在這之前的內容已被摘要取代，原文不在這個檔案裡」。

**我已驗證**：在瀏覽器中用合成資料確認標記卡出現在正確的時序位置且帶 metadata。

---

## B. 壓力與邊界（**這一段比 A 段更重要，請不要跳過**）

### B1 — 選最上層的 `projects/` 根目錄

選 `C:\Users\gunda\.claude\projects` 本身。

**預期**：列出跨專案全部約 109 筆並標出各自專案路徑；**不出現**舊的「確定要載入這麼多檔案嗎」防呆窗（那個窗已退役）；若超過 500 個檔案，清單上方要明說被略過幾個。

**我已驗證**：索引器對 218 個檔案跑過，未觸發截斷（沒到 500）。截斷訊息本身有單元測試，但**真實觸發沒測過**。

---

### B2 — 載入到一半取消，再點另一筆

在某筆載入的進度條跑動時按「取消載入」，然後立刻點清單中的另一筆。

**預期**：回到清單、清單沒有消失、第二筆正常載入、**沒有殘留的進度條**。

**我已驗證**：「載入結束（成功或失敗）都回到清單」有轉移測試；「進度條不得殘留」也有——這條測試本身就撞出一個既有洩漏（Worker 建構失敗時進度條會永遠停在「讀取中」），已一併修掉。
**為什麼還需要你**：真實的取消時序（大檔載到一半）我沒有跑過。

---

### B3 — 兩個彈窗會不會疊在一起

1. 關掉瀏覽器的 IndexedDB（DevTools → Application → Storage → Clear site data），重新整理，讓歡迎彈窗出現。
2. 在歡迎彈窗還開著時，用「載入 .jsonl」選一個**不是 transcript 的檔案**（隨便一個 `.json`）製造 fatal。

**預期**：歡迎彈窗與錯誤彈窗**不同時出現**；一個關掉之後另一個才浮出來。

**我已驗證**：在真瀏覽器中實測過同一情境——只有一個 dialog 開著、`position: fixed`（真的進了 top layer）、背景元素的命中測試回傳 dialog 本身（背景真的被擋住）、確認後排隊的歡迎彈窗自動接手。這一項我驗得最完整。

---

### B4 — 只選某個 session 自己的資料夾（舊的失敗路徑）

用「載入 .jsonl」的多選，只選 `66c03ab2-…\subagents\` 底下的檔案。

**預期**：出現一個**說得出下一步**的錯誤：「這個資料夾裡只有子代理紀錄，主檔是它的同層兄弟檔 `<id>.jsonl`……請改選上一層的專案目錄，或直接用 Session 瀏覽器挑選。」按確認可關閉，且**上一份 session 沒有被清掉**。

**我已驗證**：用真實檔案驗過會落在具名的 `NO_MAIN_TRANSCRIPT`；文案表有測試保證每個 fatal 都同時有標題與說明。

---

### B5 — Firefox 或 Safari

在 Firefox（或 Safari）重跑 A1。

**預期**：功能可用（自動退回 `webkitdirectory`），清單頂端多一行「這個瀏覽器不支援記住資料夾，每次都要重新選一次」。

**⚠ 我沒有驗證過這一項。** 後備路徑只有型別與單元層的保證，**沒有在 Firefox/Safari 實跑過**。這是本輪最可能出問題的地方。

---

### B6 — Chromium 記不記得資料夾

在 Chrome/Edge 完成 A1 之後，關掉分頁再開，點「載入 Session 資料夾」。

**預期**：直接列出上次的目錄，不需重選。若權限已被撤銷（例如你清過網站資料），應出現「需要重新授權資料夾」＋可重試，而不是白畫面。

**⚠ 我沒有驗證過這一項。** IndexedDB 存取 handle 的程式碼是 best-effort 寫法，但**跨分頁還原沒有實跑過**。

---

### B7 — 分類誤判巡檢（你最在意的那條）

把清單的「機器任務」篩選單獨打開（其餘關掉），看剩下哪些。

**預期**：真實資料裡應該**只有 1 筆**（`89a31c3c…`「Remote control implementation」，7 行的空 session，沒有任何訊息）。若你看到任何一筆其實是你自己打字的對話，那就是誤判，請回報檔名。

**我已驗證**：實測 109 個真實 session，唯一的啟發式規則（「所有 prompt 都是機器代打的固定句」）**一次都沒有觸發**——這批資料裡沒有任何一筆是靠推測分類的。過程中抓到並修掉兩個誤傷，詳見下節。

---

## C. 實測中抓到的兩個誤傷（已修，說明給你對照）

作者要求「避免誤傷」，真實資料上確實抓到兩個，**兩個都是把真人對話標成機器任務**：

| # | 症狀 | 根因 | 修法 |
|---|---|---|---|
| 1 | `3f5c5d01…`（51 則回覆的 `/doctor` 工作階段）被判 `machine` | 判準用「淨化後還剩幾個字」。斜線指令淨化後整段消失，計數變 0 | 改數「真人**出手**的回合」，與「還剩多少文字」分開；文字只用來取標題 |
| 2 | `c32fe685…`（附截圖的真人 prompt）被判 `machine` | 那則訊息把 base64 影像塞在同一行 JSON，單行 132 KB，剛好跨過 128 KB 檔頭邊界被當半行丟掉 | 殘段超過 32 KB 就不像普通一行，放大檔頭視窗（上限 1 MB）重讀一次 |

偏誤方向現在統一成一條原則並落在三處：**寧可漏判機器，不可誤判真人**——機器語句用精確比對而非前綴比對；掃描讀不到任何完整行時回報「無法判定」而非「沒有真人訊息」；同樣情況下也不得回報「不是 Claude Code」，否則檔案會從清單上憑空消失。

---

## D. 本輪已知不做 / 已知未驗

**刻意不做（有裁決紀錄）**
- **Codex 索引**（你 2026-07-28 的裁決）。Codex 的單檔載入不受影響，仍可正常使用；只是不會出現在瀏覽器清單裡，會被歸為「無法判定」而排除。
- **DSM-7/8/9/10 四項狀態機既有債**（隱私 resolver 在模組層、`cacheReady` 三個寫入點、播放 timer 在模組層、地圖同時是檢視與阻斷面）。都是 R9 之前就有的，刻意不動以免混進同一個驗收面。已登在 [`docs/BACKLOG.md`](../../BACKLOG.md)。

**已知未驗（上面標 ⚠ 的兩項）**
- **B5 Firefox / Safari 後備路徑**——完全沒有實跑過。
- **B6 Chromium 跨分頁記憶目錄**——完全沒有實跑過。

**結構性限制（不是這輪的疏漏）**
- vitest 跑 node 環境，看不到 top layer、backdrop、遮蔽與可點擊性。本輪的補法是「元件層斷言 `showModal` 真的被呼叫」＋真瀏覽器手動實測（B3 就是這樣驗的）。未來的版面／地圖工作若要自動化這一層，需要獨立的 `tests/e2e/`。已登在 BACKLOG。

---

## E. 順手修掉的既有缺陷（非你回報，但一併記錄）

1. **進度條可能永遠停在「讀取中」**——`startSessionLoad` 原本在 `try` 之外，Worker 建構失敗（CSP／`file://`／不支援 module worker）時進度條不會被清掉。寫 DSM-4 轉移測試時撞出來的。
2. **`PrivacyReview` 是假的阻斷面**——原本是 `<section role="dialog" aria-modal="true">`：不在 top layer、不畫 backdrop、不讓背景失效。送資料出去前的最後一道確認，不能只是「看起來像」擋著。已改為真正的 `<dialog>` + `showModal()`。
3. **文件裡的錯誤敘述**——`USER_GUIDE.md` 與 `architecture.md` 都寫「主檔＋`subagents/*.jsonl` 在同一資料夾」，那是 RC-1b 的文件版，一併更正。

---

## F. 完工狀態

- `main` 已含本輪全部 7 個 commit（merge commit `b107843`）。
- `npm test` 350 passed / 46 files；`npm run build` 全綠。
- 分支 `feat/r9-session-browser-and-fsm` 保留未刪。
- **尚未 push GitHub、尚未建 PR、尚未打 release**——要不要推請你說一聲。
